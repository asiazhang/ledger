# Changelog

本文件记录 Ledger 各版本对使用者可见的变更，格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/)，版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)规则。

## [Unreleased]

### Added

- **保险**：保单视角统计与到期态推导（issue #363 / ADR-0051 决策 5/6）——保单列表与编辑弹窗（详情）展示实时推导统计：累计已缴保费与累计现金流入按挂单流水忠实合计（支出记保费、理赔/退保/满期返还记收入挂单，逐笔可对账；不摊销、不落库、本位币口径），有活跃缴费协议的保单显示下期扣款日（来自协议最早未执行期次，暂停/取消不显示，价变重建后取新段），无协议不显示；到期态由保障期间推导（止日已过 → 已到期，止日为空 → 长期/终身）。软删保单不进列表与统计，历史引用不串入其他保单。不做全局年度保费口径。
- **保险**：保单建档与浏览——静态档案 CRUD（issue #360 / ADR-0051）：侧栏「资产」组新增保单独立视图（投资/物品/保单 = 金融资产/实物资产/合同权益），可新建、编辑、软删除消费型保险合同的静态档案：保险公司复用商户字典（拼音可搜，输入新名即建，同一保司全库同名一致）、保单号、险种名称、保障期间起止（止日可空 = 长期/终身）、保额（可选、纯展示不进任何金额口径，与币种成对）与备注。列表展示保司/险种/保单号/保障期间/状态（到期由止日推导，不持久化）/保额；删除为软删除，已删保单不进列表，库内行与历史引用保留不置空。可排区随保单视图扩为九项：保单默认序在资产组末位（⌘8），洞察组末位视图（搜索）落到无键位末位，右键重排可换谁无键位。本票不含缴费协议与保单统计（后续票承接）。
- **界面**：侧栏改分组形态与视图快捷键键位上限修订（issue #359 / ADR-0051）——侧栏由平铺改为「记账 / 资产 / 洞察」三组 + 概览/AI/设置三固定项：记账 = 交易、账户、预算、定时；资产 = 投资、物品（保单空位就绪，随保单建档票接入）；洞察 = 报表、搜索；分组标题不占键位、不参与排序与计数，组与组序固定、不可跨组移动。右键排序收窄为组内（上移/下移/移顶/移底/恢复默认即时生效并跨启动保持），持久化对象收窄为组内序——已存旧平铺排序数据启动时经既有脏数据防御整体回退默认序，不异常。视图快捷键键位按线性位置推导：数字键 ⌘1–⌘0 覆盖前 10 个视图（概览=1，可排区第 1–8 项=2–9，AI=0），设置为唯一例外 ⌘,；可排区容量九项，末位无键位（不出提示、不可经键盘跳转，右键重排即换谁无键位）——为保单视图预留的键位诚实化，本次仅落地语义、当前 8 个可排视图仍各有键位（分组带来默认键位变化：定时=⌘5、投资=⌘6、物品=⌘7、报表=⌘8、搜索=⌘9）。
- **概览**：新增「财务自由度」卡片（issue #344 / ADR-0048，口径聚合命令见 issue #343）——以可投资资产（持仓市值 + 投资账户现金）为分子、年度预算总额（月度预算 × 12 + 年度预算）为分母，按 3% 保守安全提取率计算自由度百分比（一位小数）；卡片展示分子/分母金额与覆盖年数，阶段标签三档（<30% 积累期 / 30–100% 接近自由 / ≥100% 财务自由），≥100% 进度条转成功；未设预算时显示占位引导、点击跳转预算页（口径不回退实际支出）；零资产显示 0%；缺汇率时卡内警告并可重试。卡片位置在投资概览卡之后、物品使用成本卡之前。
- **定时计划**：设置页新增设备级「自动执行」开关与说明文案（issue #308 / ADR-0042）——设置页新增「定时」Tab（归定时计划域，按 ADR-0022 领域归属插在「关于」之前），开关默认关，附「自动执行只应在一台机器开启」与本机设备偏好（不随备份/恢复迁移，换新机器或恢复备份后保持默认关）说明；真源在本机 localStorage 设备偏好（应用设置 store），应用启动与变更时推给后端运行时镜像（备份目录镜像推送先例）。
- **定时计划**：自动执行（到期追补）后端全链路（issue #307 / ADR-0042，部分推翻 ADR-0024 期次手动执行边界）——设备级「自动执行」开关经 IPC 推送命令更新后端运行时镜像（默认关；刻意不入设置 KV 表，不随备份/恢复迁移，换新机器默认关）；自动备份轮询线程泛化为单一 tick 双判定（备份到期判定 + 追补判定，10 分钟一轮）：开关开启时，到期期次（待执行且计划日期 ≤ 今天、计划活跃）逐条自动落账，交易日期忠实取期次计划日期，笔记本睡几天醒来一次补齐；失败/处理中/已取消期次与暂停/取消计划一律不碰，追补尝试失败的单期置为失败保持手动重试、不被自动反复重试，单期失败不中断同批后续；每笔成功经统一写入口置脏联动自动备份判定；设置页开关 UI 与说明文案由 issue #308 承接（见上条）。
- **投资**：基金申赎记账——买/卖表单选基金标的时改为照抄确认单：金额与确认份额必填（确认单为权威事实），单价（净值）由两者反算并只读展示（4 位小数），行金额恒等于确认单整分金额、不被单价舍入污染（issue #302 / ADR-0038 金额权威）；其余类型标的维持「数量 + 单价」录入不变。落库经行为层既有分派：申购建仓、赎回 FIFO 匹配、每份成本锚定权威金额单次舍入；卖出分摊双闭合（收入末匹配吸收余数、耗尽批次成本闭合到买入权威金额），全平仓后 Σ 已实现盈亏 = Σ 卖出金额 − Σ 买入金额，精确到分——持仓列表市值/未实现盈亏与盈亏页自动纳入基金（编辑申赎走全字段替换权威，持仓与盈亏随之纠正）。买卖明细投影新增标的类型，供表单按标的类型切换录入形态；AI 批量导入基金申赎（fund 标的 + 整分金额 + 份额、不传单价）同一语义自然生效。
- **投资**：标的页新增行内「录价」入口与报价弹窗（手动报价，issue #291 / ADR-0036）——无行情数据源标的（自建标的与名称充代码的基金行）按「日期 + 价格」单点录入且慢组合单位净值、华为虚拟股每股公允价：一条通道两个落点（现价缓存 upsert + 价格历史周采样幂等覆盖，同周后写覆盖先写）；回填早于最新价格点的旧价只沉淀历史、不动现价；实际写入任一落点即广播价格失效信号（价格失效信号生产者清单再添一处），标的列表、持仓概览、组合走势自动刷新。录价后自建标的的市值、未实现盈亏、净资产合计与组合走势随之显形。真实代码基金与股票无录价入口（现价归同步通道，开放录价只会被下次同步冲掉，判定口径与「净值可拉」分区同源）；价格录入支持 4 位小数（万分之一元刻度）、默认今天、价格 > 0 校验。
- **AI 导入**：新增场外基金按代码查询本地 API 端点 `GET /api/v1/funds/{code}`，并增强标的创建端点的 fund 通道（issue #304 / ADR-0039）——基金申赎账单迁移全链路：先按 6 位代码查询确认识别（返回东财权威名称、基金分类、最新单位净值（万分之一元刻度）与净值日期；非 6 位即刻拒绝不发起网络；查无此码返回中文错误）；再以真实代码创建标的，后端经东方财富校验并回填权威名称、落最新净值现价（广播价格失效信号），查无此码拒绝创建、东财网络不可达时降级为提交名称 + 真实代码建行（不阻塞导入，降级重放不覆盖既有权威名称）；最后经批量导入提交申购/赎回（幂等键去重、读回对账），名称充代码的基金行不进净值通道（仅限源数据无代码）。导入知识新增「基金申赎」节（行拆解、费用归属、标的解析差异——按代码查询优先、真实代码必带，三步法引用通用节不重述）；开放 API 契约自描述覆盖新端点与创建增强。全部端点集成测试以注入桩离线驱动。
- **投资**：「同步持仓价格」股票与基金一起刷新（issue #303 / ADR-0038 决策 6）——增量同步按类型分区：股票走既有行情通道不变，场外基金逐只走东方财富历史净值接口（带 Referer、复用主机池/限流），新买入的基金首刷即回填近两年净值周线，此后以现价缓存的净值日期为水位、从水位次日起按页增量（常态每只一页、几秒完成）；同周重复获取整周覆盖、不产生重复采样点。名称充代码的基金行与债券等无法拉净值的持仓计入「跳过 N 只」提示而非报错；单只基金首刷查无净值同样计入跳过。实际写入净值才广播价格失效信号（基金「已是最新」零变化不广播）。同步后组合走势自动纳入基金市值；单只基金可看净值走势；持仓概览现价下方展示净值日期（现价对应哪天的净值一眼可辨）。
- **投资**：标的页新增「添加基金」入口（按代码即拉，issue #301 / ADR-0038）——fund 类型标的的唯一创建通道：输入 6 位基金代码，自动从东方财富拉取名称、类型（如「混合型-灵活」）与最新单位净值回填，无需手抄任何信息；基金随即进入标的列表（拼音/代码可搜，与股票同一套语义）。现价 = 最新公布单位净值（万分之一元刻度，4 位小数保真）并携带净值日期；落现价即广播价格失效信号，未取到净值（新发基金未公布首期）仅建标的不落现价。查无此码返回中文报错且不产生标的行；重复添加复用既有标的行并刷新名称与净值（幂等）。东财基金访问收口为可注入获取函数，同步/测试链路离线可驱动。
- **AI 导入**：新增标的幂等创建本地 API 端点 `POST /api/v1/instruments`——按自然键（`symbol` + `type`）find-or-create，命中静默复用并按需更新名称/市场、返回既有 id，未命中创建新行；重复创建同一标的返回同一 id，不产生字典碎片。`symbol` 必填（源数据只有名称时以名称充当代码）；`type` 闭集五类全开（不经自建标的的 UI 类型白名单）；`name` / `market` 可选（缺省 `unknown`）；报价币种可省（缺省按市场推导：沪深→人民币、港→港币、未知→人民币，可显式覆盖）。创建行来源标记 = 手动（与自建标的同路，可走自建标的删除通道清理）；响应照账户/分类创建先例：201 + 裸 id；标的写入不触发前端参考数据重拉（标的不属参考数据）。端点契约随开放 API 自描述。
- **AI 导入**：新增标的搜索本地 API 端点 `GET /api/v1/instruments`——按关键词搜索标的字典，解除 AI 投资历史迁移的标的解析阻塞。`query` 必填（空即 400，搜索式而非全量列表）；`limit` 缺省 20、上限收敛 100（封顶返回 + 命中总数 `{items, total}` 控制上下文预算）；`market` / `type` 可选精确过滤（同码异类型标的如基金 000001 vs 股票 000001 靠 `type` 消歧）。命中语义复用统一模糊搜索（词条 AND、代码/名称子串 ∨ 拼音首字母，大小写不敏感），按 symbol 排序，返回完整 Instrument 形状；端点契约随开放 API 自描述。

### BREAKING

- **数据库 schema（就地修改 V002/V010，价格刻度翻转）**：全投资域价格语义列的刻度由「分」（1/100 元）就地重定义为**万分之一元**（0.0001 元，基金净值 4 位小数无损保真的地基，ADR-0038），波及四类列：现价（`market_prices.price_cents`）、价格历史（`price_history.price_cents`）、成交单价（`security_transactions.price_cents`）、每份成本（`security_lots.cost_per_unit_cents` 及 `security_lot_sales.cost_per_unit_cents` 成本快照）；`v_holdings` 视图表达式同步换算（金额分 = 数量 × 万分之一元单价 ÷ 100）。列名保留 `_cents` 历史名（就地改名会让新旧库列名分叉）、无 DDL 结构变化；**交易金额列（`amount_cents` 等）与手续费仍是整数分，不随本次翻转**。同一批次 V002 就地增列 `market_prices.nav_date`（净值日期：场外基金现价 = 最新公布单位净值时携带，兼任净值同步水位，ADR-0038；股票恒 NULL，#301）与 `v_holdings.latest_nav_date` 视图列（透传净值日期，持仓可见现价对应哪天的净值，#303）。就地修改只影响全新安装（全迁移即得正确刻度与净值日期列）；**存量库不在本次兼容范围**——不提供一次性 ×100 处置工具、不做迁移版本嗅探的自动修复。存量库直接升级后价格数值将按 100 倍错读（成交单价/每份成本真实记录、现价与价格历史缓存、`v_holdings` 视图口径互不匹配），且缺失 nav_date 列（基金现价写入会因列不存在而失败），处置与否由维护者自行权衡（原则见 ADR-0038 决策 5）。迁移文件头部就地修改注记见 `src-tauri/migrations/V002__investment.sql` 与 `V010__price_history.sql`。

- **数据库 schema（就地修改 V003，仅影响全新安装）**：定时交易系迁移的 9 个引用列补全显式 `ON DELETE` 动作——强依赖（账户/币种/转入账户）`RESTRICT`、溯源指针（可空分类、期次生成交易）`SET NULL`、期次与计划扩展表行（期次表、分期/订阅/定时转账三张扩展表）`CASCADE`——替代此前的 SQLite 默认 `NO ACTION`。就地修改只影响全新安装；存量库与旧备份恢复路径保持 `NO ACTION`（当前应用无硬删路径，两类 schema 行为零差异、差异不可达），未来首个依赖新 ON DELETE 语义的功能（如硬删/清理）发布时须自带收敛迁移。迁移文件头部就地修改注记见 `src-tauri/migrations/V003__scheduled_transactions.sql`。

### Changed

- **搜索**：交易搜索的金额区间筛选改按本位币分过滤（issue #395）——`amount_min_cents` / `amount_max_cents` 的过滤列由原始币种分（`amount_cents`）切换为本位币分（`amount_native_cents`），与全仓聚合口径同源：多币种生效后外币交易（如 USD 100 元折算 ¥720）按本位币金额参与跨币种混合筛选，不再与按原始分值录入的区间混滤；参数名、边界含双端与单边可用等契约形状不变，单本位币（CNY 1:1）下行为零变化。
- **分类**：删除有未删除预算的分类时明确拒绝并引导先删预算（issue #355）——错误提示「该分类名下存在预算，请先删除对应预算后再删除分类」（错误码 `category.has-budget`，双语界面同语义）；删除对应预算后该分类即可删除，名下仅剩已删除（软删）预算时不阻拦；守卫只看分类自身名下预算，删父分类不检查其子分类名下预算（「删除只影响该分类自身」既有边界不变，与分类删除不校验交易引用的现状一致）；IPC 命令与本地 HTTP 端点共用核心函数、一处生效。对已发布删除命令的行为收紧，契约形状不变，非 BREAKING。
- **投资**：价格展示与录入随刻度翻转升级——标的列表/持仓概览现价、盈亏页成本单价、单标的走势曲线按万分之一元刻度渲染（基金净值 4 位小数无损展示、股票两位价去零如常）；买卖表单单价按万分之一元录入（4 位小数可用），金额与手续费仍按分。后端金额公式同步收口：行金额 = 数量 × 万分之一元单价 ÷ 100 ± 手续费（分），买入每份成本在万分位刻度下单次舍入摊薄，不再先行舍入到分。
- **AI 导入**：导入知识与 OpenAPI 契约注明价格例外——buy/sell 的 `price_cents`（成交单价）与标的现价以万分之一元为单位（元 × 10000），行金额与手续费仍是分；AI 迁移基金申赎可无损携带 4 位小数净值。
- **AI 导入**：入口提示词最小化重构——收敛为三步骨架（发现契约 → 获取导入知识 → 按约定执行）+ 服务常驻排查提示 +「不删后重导」纪律；幂等键格式、商户建议、对账细则、纠错与错误响应等细节卸载至导入知识与 OpenAPI 契约——AI 按提示词指引，仅凭前两步（契约发现、获取导入知识）即可自足获取全部约定。全线文案清除「系统提示词」别名（提示词标题、AI 页面卡片标题、命令注释、README、OpenAPI 导入知识端点自述，后者范围描述改为「导入全流程约定」）；AI 页面说明补充「需保持 Ledger 运行」。

### Fixed

- **稳定性**：修复全量同步行情期间界面可能永久卡死的同类跨线程死锁残留（issue #369 / ADR-0054，与 issue #364 / #365 同机理）——行情同步进度事件（`sync-instruments:progress`：每页进度、失败进度与完成/中断终态）此前仍在后台同步线程上就地发射（内部持 `webviews_lock` 等主线程回执），同步进行中若主线程恰在处理界面回调即互等成环。现将全部发射点收编进与写后失效信号同一的投递机制：投递主线程事件循环队尾非阻塞执行，同步线程入队即返回；事件名、payload 与先后顺序不变（允许毫秒级延迟），发射失败静默忽略、不影响同步结果。

- **稳定性**：修复批量导入等写操作触发界面永久卡死的跨线程死锁（issue #364 / #365 / ADR-0054）——写后失效信号发射由「写线程同步 `app.emit`（内部持 `webviews_lock` 等主线程回执）」改为投递主线程事件循环队尾非阻塞执行：写请求（应用内操作与本地 HTTP 写端点，含 `/api/v1/transactions/batch` 批量导入）及时返回，不再因信号发射无限挂起；信号在主线程队列依次执行，参考表 / 价格 / 备份列表照常自动刷新（允许毫秒级延迟，最终到达）；发射失败（如应用退出中）静默忽略，不影响已成功的写库结果。机制在信号发射单点一处改动，IPC 壳、HTTP 壳与自动备份深路径全壳生效；「哪个写操作发哪个信号」的判定（ADR-0044）零变化。

- **AI 导入**：修复经本地 HTTP API 导入带新商户名的交易后，前端商户字典不自动刷新的失效信号漏发——交易写入「即建商户」（入参带 `merchant_name` 且未命中时后端即建商户行）现在把「是否即建」作为写操作证据随创建/修改入口外传，单笔创建、批量导入（按批聚合「任一行即建」）、修改三条写路径（IPC 与 HTTP 两壳）在即建商户时发出 `ledger:changed` 参考失效信号，前端参考数据 store 据此自动重拉商户字典；仅命中复用（名字命中或直接带商户 id）与纯交易写保持零信号（issue #331 / ADR-0044 决策 4）。

- **AI 导入**：buy/sell 引用不存在的标的时，由扩展表外键违规的「数据库错误」（HTTP 500、批量导入整批回滚）改为行为层 prepare 拦截的 400 中文错误——「买入/卖出标的不存在: <标的 id>」，AI 可读错误回自纠（搜索标的或创建后重试）；批量导入降为逐行失败（`success: false` + `error`），不再整批回滚、不影响同批其他行。创建与修改（全字段替换）路径同样生效；既有校验（必须指定标的、数量/单价大于 0、投资账户约束、可卖数量等）行为与错误信息不变。

- **日志**：修复「设置 → 关于」点击「打开日志目录」始终报错「log.open_log_dir not allowed. Plugin not found」——该命令原承载于自研内联日志插件，其 ACL 权限从未配置，调用在权限层即被拒绝、从未到达 Rust 函数，自 v0.3.0 发布以来该按钮一次都未成功过。现将「打开日志目录」改为普通 Tauri 命令（免 ACL、自动纳入统一 IPC 调用日志），前端调用收口进 api 对象，错误提示由后端中文前缀唯一承担。IPC 调用名由 `plugin:log|open_log_dir` 变为 `open_log_dir`；原调用名从未成功返回过、不存在可受影响的调用方，故不构成 BREAKING。

## [0.3.0] - 2026-08-27

### Added

- **交易过滤**：交易页顶部新增过滤行——账户下拉（涉及账户语义，可清除）、日期起止、交易类型下拉（可清除）与一键清除按钮；多条件可组合，分页/页大小切换保持过滤条件，过滤无结果时展示空态提示。手动过滤不写回 URL（组件状态为唯一事实源，从侧边栏重新进入即回到全量列表）。
- **账户名下钻**：交易列表与搜索页的账户列渲染为可点击账户名（琥珀强调色、hover 亮琥珀 + 下划线 + 背景微亮、键盘可达），点击跳转交易页并按涉及账户过滤；转账行显示「转出 → 转入」双向账户名，两个名字各自可点击、各自跳转到对应账户的过滤视图。账户页名称列沿用同一视觉与跳转行为。黑洞账户（余额恒 0 的汇总账户）渲染为纯文本「-」，不可点击。
- **搜索**：新增搜索视图——按关键字检索备注与转出账户名（整词精确匹配），支持金额区间与日期范围筛选，可组合使用；结果列表展示账户/金额/日期等列。
- **分页**：交易列表与投资标的列表改为服务端分页（items + total），支持页大小切换，空页自动回退到上一页。
- **备份**：新增文件级备份与恢复功能（设置页），支持备份保留上限与自动滚动清理。
- **导入**：批量导入按客户端幂等键去重（内容哈希降为兜底）；新增按 id 全字段替换交易接口 `PUT /transactions/{id}`，AI 可按 id 修改纠错。
- **快捷键**：视图切换快捷键（Cmd/Ctrl+1..9）。
- **状态记忆**：持久化窗口位置/大小与各视图的查看状态。

### Changed

- **外观**：主题定制化（Raycast 精致工具感）——强调色改为琥珀暖橙、圆角提升至 8/12px、暗色背景分层为近黑底+细边框；收入/支出/退款语义色保持不变。评估了迁移至 Shadcn Vue + Tailwind 的路线后决定留在 Naive UI，改用 `theme-overrides`（`src/theme/overrides.ts`）定制，迁移成本（约 1.5–2 周）与收益不匹配。
- **日志**：数据库操作耗时日志上线——全量 debug 级 + 慢查询 warn 级，SQL 耗时归因到调用命令；导入批次打 info 汇总日志（总耗时 + 交易条数 + 失败数）。

### Fixed

- **分类**：修复界面无法显示外部新增的分类数据。
- **交易列表**：修复列宽被拉伸及备注溢出显示，改为备注为唯一弹性列。
- **界面**：禁用 overscroll 橡皮筋，消除滚动白条。

## [0.2.0] - 2026-08-22

### Added

- **AI 导入**：新增 AI 系统提示词视图，可在应用内查看并一键复制 AI 导入入口提示词（供 Cursor、Claude Code 等 AI 助手使用），提示词指引 AI 通过 HTTP API 自行发现端点与获取导入约定

## [0.1.1] - 2026-08-22

### Changed

- **依赖**：升级后端 Rust 依赖，HTTP 客户端切换为 rustls

### Fixed

- **构建**：修复 clippy 警告，CI 补充后端检查

## [0.1.0] - 2026-08-22

### Added

- **账本**：首个可用版本——多币种账户、支出/收入/转账记录、分类管理（图标/颜色/排序/二级层级）、预算与报表视图
- **投资**：投资账户与股票标的、买卖交易、FIFO 卖出匹配与已实现盈亏、现价展示、东方财富全量同步
- **AI 导入**：本地 HTTP API（/api/v1）供 AI 助手幂等写入账户/分类/交易，支持批量去重与黑洞账户，附 OpenAPI 文档与导入知识说明
- **计划交易**：定时交易模块（订阅/分期/转账）
- **日志**：按天滚动日志，设置页可打开日志目录
- **发布**：GitHub Actions 构建 macOS DMG，打 v* tag 自动发布 GitHub Release

### Fixed

- **同步**：股票同步分页截断导致港股漏抓、跨市场进度条重置、启动崩溃（tokio 运行时上下文）、JPY 精度等问题
