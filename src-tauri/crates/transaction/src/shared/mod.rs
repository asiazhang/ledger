//! 共享语义区目录 `shared`（ADR-0113 决策 2 目录约定 / #1597）：共享语义独立
//! 模块的区住址——统一模糊搜索语义纯函数住 `search_text`（ADR-0027 全库唯一定
//! 义点，被本域读路径与投资域下拉共同消费）。区归属 = 顶层目录：`shared/` 与
//! `amount/`、`model/`、`command/` 同属共享语义区，依赖面仅限共享语义区内。
//!
//! 目录内 mod.rs 只做声明与逐项再导出（ADR-0113 决策 5）。

pub mod search_text;
