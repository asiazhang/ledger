Feature: 加密最小闭环——开启加密、重启、解锁（issue #570 / ADR-0075）
  用户第一次真正用上加密：设置页开启加密模式并设置主口令，整库一次性转换
  （先产出并验证新库、原子替换、旧文件保留副本），完成后自动重启；启动
  探测发现密文库，前端解锁屏手输主口令进入应用。解锁先于一切业务读写；
  口令错误不等于库损坏，可无限重试；加密库搬迁后仍是加密库；忘记口令
  逃生门重置为全新明文库、旧库保留密文副本；明文模式启动流程零改动。
  真临时目录文件库驱动真实文件系统（先例：data_location）。

  Scenario: 明文库启动探测不进入锁定（明文启动路径零改动）
    Given 默认数据目录中已有一个含 3 条交易的明文库
    When 执行启动引导并探测库文件
    Then 库文件应探测为明文库
    And 引导不应等待解锁
    And 引导不应发生回退

  Scenario: 开启加密——整库一次性转换，原库保留副本，数据完整无损
    Given 默认数据目录中已有一个含 3 条交易的明文库
    When 用主口令 "correct horse" 开启加密
    Then 转换应成功
    And 原库文件应保留为 .bak 明文副本
    And 库文件应探测为密文库
    And 凭主口令 "correct horse" 打开当前库应包含 3 条交易且内容完整

  Scenario: 转换中途失败时原库原样保留，不存在半加密状态
    Given 默认数据目录中已有一个含 2 条交易的明文库
    And 记录当前库文件字节
    And 数据目录不可写
    When 用主口令 "pw" 开启加密
    Then 转换应失败
    And 当前库文件字节应保持不变且仍为明文库
    And 原库仍能以明文打开且包含 2 条交易
    And 目录中不应残留转换临时文件或 .bak 副本

  Scenario: 启动探测发现密文库，正确口令解锁进入应用
    Given 默认数据目录中有一个凭主口令 "pw" 加密且含 3 条交易的密文库
    When 执行启动引导并探测库文件
    Then 库文件应探测为密文库
    When 以主口令 "pw" 解锁
    Then 解锁应成功且打开的库应包含 3 条交易
    And 凭主口令 "pw" 可再次打开当前库

  Scenario: 错误口令提示重试且不锁定、不损坏库文件
    Given 默认数据目录中有一个凭主口令 "pw" 加密且含 3 条交易的密文库
    And 记录当前库文件字节
    When 以主口令 "wrong" 解锁
    Then 解锁应失败且错误码为 "encryption.passphrase-incorrect"
    And 当前库文件字节应保持不变
    When 以主口令 "pw" 再次解锁
    Then 解锁应成功且打开的库应包含 3 条交易

  Scenario: 忘记口令逃生门——重置为全新明文库，旧库保留密文副本
    Given 默认数据目录中有一个凭主口令 "pw" 加密且含 3 条交易的密文库
    When 执行忘记口令重置
    Then 重置应成功
    And 库文件应探测为明文库
    And 重置后的新库应为不含交易的明文空库
    And 原密文库应保留为 .bak 密文副本
    And .bak 密文副本凭原主口令 "pw" 仍可打开且包含 3 条交易

  Scenario: .bak 密文副本无密钥不可读
    Given 默认数据目录中有一个凭主口令 "pw" 加密且含 3 条交易的密文库
    When 执行忘记口令重置
    Then 重置应成功
    When 不带口令打开 .bak 密文副本
    Then 打开应失败且错误为文件不可读

  Scenario: 重置后可再次走开启加密流程
    Given 默认数据目录中有一个凭主口令 "pw" 加密且含 3 条交易的密文库
    When 执行忘记口令重置
    Then 重置应成功
    When 用主口令 "new pw" 开启加密
    Then 转换应成功
    And 库文件应探测为密文库
    And 凭主口令 "new pw" 打开当前库应包含 0 条交易且内容完整

  Scenario: 密文库搬迁等待解锁后补做，目标库仍为密文库
    Given 默认数据目录中有一个凭主口令 "pw" 加密且含 3 条交易的密文库
    And 指针文件指向空的目标目录
    When 执行启动引导
    Then 生效目录应为默认数据目录
    And 引导应等待解锁后搬迁到目标目录
    When 以主口令 "pw" 解锁并补做等待中的搬迁
    Then 搬迁应成功
    And 目标目录的库应探测为密文库
    And 凭主口令 "pw" 打开目标目录的库应包含 3 条交易且内容完整
    And 源目录的密文库应原样保留
