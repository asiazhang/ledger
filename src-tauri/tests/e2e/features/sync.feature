Feature: 多端同步单端旅程（spec #863 / ADR-0091 决策 9）
  单端用户旅程：配置通道 → 触发同步 → 数据落库 → 挂起通知可见。
  真双端语义（LWW / 防双扣 / 重放幂等）归域单测（ADR-0087），此处不为域规则凑数据；
  步骤直调域层公开接缝（轮次编排、会话信封形态、通道取件），不触网真网盘。

  Scenario: 配置通道后打开应用即同步把本机数据推上通道
    Given 存在账户 "现金" 类型 "cash" 币种 "CNY"
    And 写入一笔支出 1500 到账户 "现金" 日期 "2026-02-01" 备注 "午餐"
    When 以当前账本配置同步通道 空间 "default"
    And 打开应用即同步一轮
    Then 通道上应有本机账本目录
    And 本轮同步应上传 2 条操作

  Scenario: 手动触发同步后通道上的对端数据落到本端
    Given 存在账户 "现金" 类型 "cash" 币种 "CNY"
    And 写入一笔支出 1500 到账户 "现金" 日期 "2026-02-01" 备注 "午餐"
    And 以当前账本配置同步通道 空间 "default"
    And 对端账本已把同一笔数据推上同一通道
    When 手动触发一轮同步
    Then 本端应已应用对端操作
    And 交易列表应包含 2 条记录

  Scenario: 同步轮次成功后回显上次同步时刻
    Given 存在账户 "现金" 类型 "cash" 币种 "CNY"
    And 以当前账本配置同步通道 空间 "default"
    When 打开应用即同步一轮
    Then 同步状态应显示已配置通道
    And 同步状态应带上次同步时刻

  Scenario: 通道未配置时自动同步零动作且本地记账不受影响
    Given 存在账户 "现金" 类型 "cash" 币种 "CNY"
    When 打开应用即同步一轮
    Then 自动同步应零动作
    And 同步状态应显示未配置通道

  Scenario: 不可重放操作进入挂起队列并可见（挂起通知）
    Given 存在账户 "现金" 类型 "cash" 币种 "CNY"
    And 写入一笔支出 1500 到账户 "现金" 日期 "2026-02-01" 备注 "午餐"
    And 以当前账本配置同步通道 空间 "default"
    And 对端投递一条引用不存在账户的操作
    When 手动触发一轮同步
    Then 挂起队列应有 1 条不可重放操作
    And 挂起通知应携带码化原因

  Scenario: 密文库会话形态下自动同步以会话口令封包（不读钥匙串）
    Given 存在账户 "现金" 类型 "cash" 币种 "CNY"
    And 以当前账本配置同步通道 空间 "default"
    When 以密文库会话形态打开应用即同步一轮
    Then 会话信封形态应为密文
    And 自动同步应封包上传

  Scenario: 同步失败不影响本地记账（错误可辨且数据完整）
    Given 存在账户 "现金" 类型 "cash" 币种 "CNY"
    And 写入一笔支出 1500 到账户 "现金" 日期 "2026-02-01" 备注 "午餐"
    And 以当前账本配置同步通道 空间 "default"
    And 通道指向不可达的同步地址
    When 手动触发一轮同步
    Then 应返回错误码 "sync-channel.network-failed"
    And 交易列表应包含 1 条记录
