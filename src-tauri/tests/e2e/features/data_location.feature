Feature: DataLocation 启动引导（issue #132）
  库外指针文件 + 启动期定位/搬迁：未配置走默认目录；合法更改意图在下次启动时
  把整库搬迁到新位置且旧库原样保留；指针损坏或目标不可用回退默认目录并给出
  回退信号；启动失败重置兜底不受影响。

  Scenario: 未配置时使用默认目录
    Given 默认数据目录中已有一个含 2 条交易的库
    When 执行 DataLocation 引导并打开数据库
    Then 生效目录应为默认数据目录
    And 不应发生回退
    And 打开的库应包含 2 条交易

  Scenario: 合法更改意图在下次启动时搬迁整库
    Given 默认数据目录中已有一个含 2 条交易的库
    And 指针文件指向目标目录
    When 执行 DataLocation 引导并打开数据库
    Then 生效目录应为目标目录
    And 不应发生回退
    And 目标目录的库应包含 2 条交易
    And 目标目录的库的关键表内容应与默认目录的库一致
    And 默认数据目录的库应原样保留且仍包含 2 条交易

  Scenario: 目标位置已有同名库时直接接管使用
    Given 默认数据目录中已有一个含 2 条交易的库
    And 指针文件指向已含 1 条交易库的目标目录
    When 执行 DataLocation 引导并打开数据库
    Then 生效目录应为目标目录
    And 不应发生回退
    And 打开的库应包含 1 条交易
    And 目标目录的库应包含 1 条交易
    And 默认数据目录的库应原样保留且仍包含 2 条交易

  Scenario: 源库损坏时搬迁失败回退默认目录，重置兜底仍可介入
    Given 默认数据目录中存在一个损坏的库文件
    And 记录默认数据目录库文件的字节
    And 指针文件指向目标目录
    When 尝试执行 DataLocation 引导并打开数据库（预期打开失败）
    Then 生效目录应为默认数据目录
    And 回退信号应包含 "搬迁"
    And 默认数据目录库文件的字节应保持不变
    When 执行启动失败重置
    Then 重置后的库应为空库（0 条交易）
    And 原库文件应被重命名为 .bak 保留

  Scenario: 二次启动幂等，已就位则不再搬迁
    Given 默认数据目录中已有一个含 2 条交易的库
    And 指针文件指向目标目录
    When 执行 DataLocation 引导并打开数据库
    And 在生效位置的库中记入 1 条标记交易
    When 执行 DataLocation 引导并打开数据库
    Then 生效目录应为目标目录
    And 不应发生回退
    And 打开的库应包含 3 条交易
    And 默认数据目录的库应原样保留且仍包含 2 条交易

  Scenario: 指针损坏时回退默认目录并给出回退信号
    Given 默认数据目录中已有一个含 2 条交易的库
    And 指针文件内容为损坏文本 "{not valid json"
    When 执行 DataLocation 引导并打开数据库
    Then 生效目录应为默认数据目录
    And 回退信号应包含 "指针"
    And 打开的库应包含 2 条交易
    And 默认数据目录的库应原样保留且仍包含 2 条交易

  Scenario: 指针目标不可用（无法创建目录）时回退默认目录
    Given 默认数据目录中已有一个含 2 条交易的库
    And 指针文件指向一个无法创建的目标目录
    When 执行 DataLocation 引导并打开数据库
    Then 生效目录应为默认数据目录
    And 回退信号应包含 "目标"
    And 打开的库应包含 2 条交易
    And 默认数据目录的库应原样保留且仍包含 2 条交易

  Scenario: 源空且目标空时在目标位置新建空库
    Given 空的默认数据目录
    And 指针文件指向目标目录
    When 执行 DataLocation 引导并打开数据库
    Then 生效目录应为目标目录
    And 不应发生回退
    And 打开的库应为空库（0 条交易）

  Scenario: 启动失败重置兜底仍工作
    Given 默认数据目录中存在一个损坏的库文件
    When 执行启动失败重置
    Then 重置后的库应为空库（0 条交易）
    And 原库文件应被重命名为 .bak 保留

  # ---- 领域命令层（issue #133）：信息查询 / 更改意图校验提交 / 恢复默认 ----

  Scenario: 向未占用的新目录提交更改意图，校验通过且意图落盘
    Given 默认数据目录中已有一个含 2 条交易的库
    When 向一个未占用的新目录提交更改意图
    Then 提交结果应为意图已落盘
    And 指针文件应指向目标目录

  Scenario: 目录无法创建时提交被拒绝，意图不落盘
    Given 默认数据目录中已有一个含 2 条交易的库
    When 向一个无法创建的目录提交更改意图
    Then 提交应被拒绝且错误信息包含 "目标目录"
    And 指针文件应保持未配置

  Scenario: 目录存在但不可写时提交被拒绝，意图不落盘
    Given 默认数据目录中已有一个含 2 条交易的库
    When 向一个只读目录提交更改意图
    Then 提交应被拒绝且错误信息包含 "不可写"
    And 指针文件应保持未配置

  Scenario: 目标已有同名库时返回二选一信号，选择接管后意图落盘
    Given 默认数据目录中已有一个含 2 条交易的库
    And 一个已含 1 条交易库的目标目录
    When 向该目标目录提交更改意图（不接管既有库）
    Then 提交结果应为需要二选一
    And 指针文件应保持未配置
    When 选择接管既有库并再次提交
    Then 提交结果应为意图已落盘
    And 指针文件应指向目标目录
    And 目标目录的库应包含 1 条交易

  Scenario: 目标已有同名库时取消换位则状态不变
    Given 默认数据目录中已有一个含 2 条交易的库
    And 一个已含 1 条交易库的目标目录
    When 向该目标目录提交更改意图（不接管既有库）
    Then 提交结果应为需要二选一
    And 指针文件应保持未配置
    And 目标目录的库应包含 1 条交易
    And 默认数据目录的库应原样保留且仍包含 2 条交易

  Scenario: 恢复默认位置与更改走同一校验提交机制
    Given 默认数据目录中已有一个含 2 条交易的库
    And 指针文件指向目标目录
    When 执行 DataLocation 引导并打开数据库
    Then 生效目录应为目标目录
    When 提交恢复默认位置（不接管既有库）
    Then 提交结果应为需要二选一
    And 指针文件应指向目标目录
    When 选择接管既有库并再次提交恢复默认
    Then 提交结果应为意图已落盘
    And 指针文件应指向默认数据目录

  Scenario: 查询 DataLocation 信息（未配置，无警示无待生效）
    Given 默认数据目录中已有一个含 2 条交易的库
    When 执行 DataLocation 引导并打开数据库
    And 查询 DataLocation 信息
    Then 信息中的生效目录应为默认数据目录
    And 信息应无待重启生效状态且无回退警示

  Scenario: 意图落盘后重启前信息显示已更改待重启生效，重启后恢复
    Given 默认数据目录中已有一个含 2 条交易的库
    When 执行 DataLocation 引导并打开数据库
    And 向一个未占用的新目录提交更改意图
    And 查询 DataLocation 信息
    Then 信息应显示已更改待重启生效，意图目录为目标目录
    When 执行 DataLocation 引导并打开数据库
    And 查询 DataLocation 信息
    Then 信息应无待重启生效状态且无回退警示

  Scenario: 回退警示在信息中透出
    Given 默认数据目录中已有一个含 2 条交易的库
    And 指针文件内容为损坏文本 "{not valid json"
    When 执行 DataLocation 引导并打开数据库
    And 查询 DataLocation 信息
    Then 信息应携带回退警示包含 "指针"
    And 信息应无待重启生效状态

  Scenario: 目标即当前生效目录时接管提交为幂等意图，不产生待重启状态
    Given 默认数据目录中已有一个含 2 条交易的库
    When 执行 DataLocation 引导并打开数据库
    And 向当前生效目录提交更改意图（不接管既有库）
    Then 提交结果应为需要二选一
    When 选择接管既有库并再次提交
    Then 提交结果应为意图已落盘
    And 指针文件应指向默认数据目录
    When 查询 DataLocation 信息
    Then 信息应无待重启生效状态且无回退警示
