Feature: 启动失败恢复（issue #601）
  启动期数据库打不开（明文库损坏等）不再弹原生「重置/退出」对话框、不再退出：
  启动失败状态暴露给前端，由启动失败恢复屏承担恢复通道——首版通道为「重置为
  空库」（旧库按既有重置命名语义保留 `.bak` 副本）。同时落两个恢复通道的前置
  修复：恢复目标路径经 DataLocation 引导的库目录（修掉写死默认目录的缺陷），
  且无已打开库连接时恢复照常可用。

  # ---- 启动期处置接管（失败恢复屏的后端判定接缝：文件判定 + 建连复合结果） ----

  Scenario: 头部完好的损坏明文库启动进入失败状态
    Given 默认数据目录中存在一个头部完好但内容损坏的明文库
    And 记录默认数据目录库文件的字节
    When 按启动处置流程尝试接管库文件
    Then 启动应进入失败状态

  Scenario: 非密文形态的损坏残留不进入解锁等待，启动进入失败状态
    Given 默认数据目录中存在一个损坏的库文件
    When 按启动处置流程尝试接管库文件
    Then 启动应进入失败状态

  Scenario: 真密文库启动进入等待解锁（加密启动路径零改动）
    Given 默认数据目录中有一个凭主口令 "passphrase-123" 加密且含 1 条交易的密文库
    When 按启动处置流程尝试接管库文件
    Then 启动应进入等待解锁

  Scenario: 完好明文库启动正常打开（明文日常启动零改动）
    Given 默认数据目录中已有一个含 2 条交易的库
    When 按启动处置流程尝试接管库文件
    Then 启动应正常打开
    And 打开的库应包含 2 条交易

  # ---- 失败 → 重置 → 进入空账本（恢复通道①，旧库 .bak 保留） ----

  Scenario: 明文库损坏启动失败后重置为空库，旧库保留 .bak 副本
    Given 默认数据目录中存在一个头部完好但内容损坏的明文库
    And 记录默认数据目录库文件的字节
    When 按启动处置流程尝试接管库文件
    Then 启动应进入失败状态
    When 执行启动失败重置
    Then 重置后的库应为空库（0 条交易）
    And 原库文件应被重命名为 .bak 保留
    And 重置保留的 .bak 副本应与原库字节一致

  # ---- 恢复通道前置修复：目标路径经引导目录解析 ----

  Scenario: 生效目录解析优先引导登记，未登记回退默认目录
    Given 默认数据目录中已有一个含 2 条交易的库
    And 指针文件指向目标目录
    When 执行 DataLocation 引导并打开数据库
    Then 引导登记的生效库目录应为目标目录
    When 以未登记引导解析生效库目录
    Then 解析出的生效库目录应为默认数据目录

  # ---- 备份恢复通道（issue #602 / ADR-0075 决策 5 修订）：失败恢复屏的
  # 「从备份文件恢复…」走既有 Restore 全语义：RestoreSafetyBackup 字节副本、
  # 原子替换、密文备份凭主口令校验（口令错误可重试、原库可回滚）----

  Scenario: 明文库损坏启动失败后从备份恢复，数据完整无损且安全备份保留失败前现场
    Given 默认数据目录中存在一个头部完好但内容损坏的明文库
    And 一份含 2 条交易的明文库备份
    And 记录默认数据目录库文件的字节
    When 按启动处置流程尝试接管库文件
    Then 启动应进入失败状态
    When 从备份恢复到启动失败的库位置（无已打开库连接参与）
    Then 恢复的数据库应包含 2 条交易
    And 恢复安全备份应与失败前库字节一致

  Scenario: 失败场景恢复密文备份：错误口令被拒原库可回滚，正确口令恢复后数据完整
    Given 默认数据目录中存在一个头部完好但内容损坏的明文库
    And 记录默认数据目录库文件的字节
    And 以主口令 "pw" 加密的文件库中已有账户 "现金" 与 2 条交易
    When 备份数据库到临时文件
    And 按启动处置流程尝试接管库文件
    Then 启动应进入失败状态
    When 尝试以主口令 "wrong" 从备份恢复到启动失败的库位置
    Then 恢复应失败且错误码为 "encryption.passphrase-incorrect"
    And 启动失败库位置的文件字节应保持不变
    And 拒绝场景不应生成恢复安全备份
    When 以主口令 "pw" 从备份恢复到启动失败的库位置
    Then 恢复的数据库应探测为密文库
    And 凭主口令 "pw" 打开恢复的数据库应包含 2 条交易
    And 恢复安全备份应与失败前库字节一致

  # ---- 恢复通道前置修复：自定义数据位置下恢复写入引导目录、无连接可用 ----

  Scenario: 恢复写入引导目录（自定义数据位置不错位）且全程无已打开库连接参与
    Given 指针文件指向目标目录
    And 目标目录中存在一个损坏的库文件
    And 存在账户 "现金" 类型 "cash" 币种 "CNY"
    When 创建交易 类型 "expense" 金额 1500 到账户 "现金" 日期 "2026-02-01" 备注 "午餐"
    And 备份数据库到临时文件
    When 执行 DataLocation 引导（不建连）
    Then 生效目录应为目标目录
    When 从备份恢复到生效目录的库位置（无已打开库连接参与）
    Then 目标目录的库应包含 1 条交易
    And 默认数据目录中不存在库文件

  # ---- 解锁屏恢复入口（issue #603 / ADR-0075 决策 5 修订）：密文库等待解锁
  # 期间，「从备份文件恢复」与「忘记口令」并列常驻——恢复通道在锁定门白名单
  # 内可达，走既有 Restore 全语义（安全备份字节副本、原子替换），成功后自动
  # 重启由启动探测接管实际模式；手输过的口令作为上下文口令自动试开密文备份，
  # 失败可就地重输（口令错误不改动等待解锁的库文件任何字节）----

  Scenario: 密文库等待解锁时从备份恢复明文备份，恢复后重启进入明文数据
    Given 默认数据目录中有一个凭主口令 "vault-pw" 加密且含 3 条交易的真密文库
    And 一份含 2 条交易的明文库备份
    And 记录默认数据目录库文件的字节
    When 按启动处置流程尝试接管库文件
    Then 启动应进入等待解锁
    When 从备份恢复到等待解锁的库位置（解锁屏恢复入口）
    Then 恢复的数据库应包含 2 条交易
    And 恢复安全备份应与失败前库字节一致
    When 按启动处置流程尝试接管库文件
    Then 启动应正常打开
    And 打开的库应包含 2 条交易

  Scenario: 解锁屏恢复密文备份：错误口令被拒不改库字节，正确口令恢复后数据完整
    Given 默认数据目录中有一个凭主口令 "vault-pw" 加密且含 3 条交易的真密文库
    And 以主口令 "pw" 加密的文件库中已有账户 "现金" 与 2 条交易
    And 记录默认数据目录库文件的字节
    When 按启动处置流程尝试接管库文件
    Then 启动应进入等待解锁
    When 备份数据库到临时文件
    And 尝试以主口令 "wrong" 从备份恢复到等待解锁的库位置
    Then 恢复应失败且错误码为 "encryption.passphrase-incorrect"
    And 等待解锁库位置的文件字节应保持不变
    And 拒绝场景不应生成恢复安全备份
    When 以主口令 "pw" 从备份恢复到等待解锁的库位置
    Then 恢复的数据库应探测为密文库
    And 凭主口令 "pw" 打开恢复的数据库应包含 2 条交易
    And 恢复安全备份应与失败前库字节一致
