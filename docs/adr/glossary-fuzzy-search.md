# 模糊搜索术语表

## Search Document（搜索文档）

一笔交易在搜索索引中的可搜索表示。它把交易本身的备注、关联账户名、关联分类名以及它们的中文拼音首字母拼接成一段文本，供 FTS5 全文索引使用。

## Searchable Content（可搜索内容）

搜索文档中实际被 FTS5 索引的文本。生成规则：

```
note + 账户名 + 分类名 + 账户名拼音首字母 + 分类名拼音首字母 + 备注拼音首字母
```

例如一笔「吃饭」交易，关联账户「招商银行」、分类「餐饮」：

```
"吃饭 招商银行 餐饮 zsyh cy cf"
```

## Pinyin Initial（拼音首字母）

中文文本每个字的拼音首字母串。用于支持用户输入拼音首字母匹配中文内容，如 `cf` 匹配「吃饭」。由 Rust 层在写入索引时生成并统一转小写。

## FTS5 Virtual Table（FTS5 虚拟表）

SQLite 提供的全文搜索虚拟表。本设计使用 `search_transactions` 作为 contentless 虚拟表，只保存可搜索文本和交易 ID 元数据，不重复存储完整交易行。

## Reindex（重建索引）

当交易、账户名或分类名发生变化时，重新生成对应交易的搜索文档并写入/更新 FTS5 索引的过程。应用层负责调用；SQLite 触发器作为兜底机制。

## Soft-Delete Filter（软删除过滤）

所有搜索结果必须排除 `is_deleted = 1` 的记录。FTS5 索引本身不感知软删除状态，查询时通过 JOIN 主表并加 `is_deleted = 0` 条件实现。

## Rank（相关性得分）

FTS5 根据词频、字段权重等计算出的匹配得分。搜索结果默认按 `rank` 降序，再按交易日期降序。

## Match Query（匹配查询）

用户输入经过分词/预处理后传给 FTS5 `MATCH` 子句的表达式。例如用户输入 `cf 吃饭` 会被转换为 `cf OR 吃饭`，以便同时命中拼音首字母和原始中文。

## 参考

- ADR-005：`docs/adr/005-fuzzy-search-transactions.md`
