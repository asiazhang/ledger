# ADR 索引

收录标准（三问，缺一不立）：**难以逆转**（改主意的代价大）、**事后会困惑**（不看记录无法理解为什么这么做）、**真实取舍**（存在货真价实的备选项）。

纯实现注记、UI 微调、已被翻案且被后续 ADR 完整转述的决策不立 ADR。正文只留决策、理由、否决方案与证据（事故、负向验证、发布边界事实）；实施过程、迁移对账与逐票落地记录归提交信息与 PR，不在正文复述。交叉引用了已删除编号的，见文末「已删除」清单。

## 现行 ADR

### 数据模型与核心口径
- 0001 — 定时交易采用独立 ScheduledTransaction + Occurrence 模型（计划与已发生交易分离）
- 0003 — 定时计划核心表 + 扩展表（现行五表结构）
- 0011 — kind→度量矩阵与 raw/native 金额分离，收口 transaction 领域模块（全库金额口径唯一真源；部分被修订：折算时点改为交易日期 + 折算来源留痕，见修订记录）
- 0013 — 交易类型行为收敛分派 + investment 出口收窄为 prepare/apply/revert（kind 行为单点、三件套出口；部分被 0033/0097 修订）
- 0019 — 价格历史化：K 线回填 + 周粒度采样，MarketPrice 收缩为现价缓存（部分被修订：汇率序列换源 ECB、窗口与标的 K 线解耦，见修订记录）
- 0020 — 首页净资产 = 非投资账户余额 + 持仓市值（防重复计算）
- 0026 — 余额调整采用黑洞转账机制
- 0028 — 交易对手方统一为商户 Merchant（breaking，推翻 0024 一个决策点）
- 0029 — 预算永久滚动（start_date 为冻结残留）
- 0038 — 场外基金一等标的：复用 buy/sell，价格刻度就地改万分位（BREAKING）
- 0048 — 财务自由度口径：可投资资产 × 3% ÷ 年度预算总额
- 0052 — 预算彼此独立：父子并存不扣减不互斥（双计是有意的）
- 0053 — 个人间借贷复用转账与债权/债务账户，不新增 kind
- 0067 — 余额与净资产持久化缓存：写路径同事务整体重算 + 读探针指纹自愈（推翻 ADR-0019「个人规模收益趋零」前提）
- 0089 — 多账本立项：一库 = 一本账，多库文件经引导层实现，schema 永不加账本维度（显式修订 0018 后果条款）
- 0114 — 跨账本投资汇总：多库只读聚合、只覆盖投资域口径、按主账本位币当期汇率折算、加密本未解锁不计入（ADR-0089「不跨本汇总」的投资域例外；grilling 定稿，2026-09-12）
- 0092 — transfer 放开携带商户：借贷转账可关联商户（修订 0053 决策 2 的商户部分）
- 0096 — buy/sell 增设出资账户：直扣买卖的资金流归因（关联 0038/0067）
- 0099 — 基金转换（convert）：第 9 种 kind（一笔一行两腿、六度量系数全 0）、转入成本按转出批次结转、转出消耗表承载回退依据；schema 就地修改 V001/V002（1.0 前 BREAKING，存量库须重建库；spec #973）
- 0106 — 份额调整（split）：激活已声明 kind——单标的非现金份额变动（折算/结转/送股为 +、缩股为 −）、批次成本按比例重述（尾差归末批次）、批次重述审计表承载精确回退；零 CHECK 改动、零 BREAKING；无现金腿 kind 界面只读化（AI 记账为写入主面；修订 0099 决策 1/7/8；spec #1045、#1048）
- 0109 — 现金分红（dividend）：激活已声明 kind——归属标的的现金收入（现金腿入任意在用账户）、不计入成本法；累计收益扩为「未实现 + 已实现 + 累计分红」三腿；零 schema 改动；有现金腿 kind 同样界面只读（AI 记账为写入主面；#1078）
- 0115 — 投资收益率采用资金加权（XIRR）口径：实际现金流 + 期末市值解年化；基金转换两腿按结转成本补现金流；区间选择以期初市值作为输入假设；缺价跳过、按币种分组不折算（grilling 定稿，2026-09-12）
- 0129 — 盈亏页新增「已实现收益」口径：按年/按账户表各增一列主值（已实现盈亏 + 现金分红）、单元格内以副行拆出两腿，两腿定义逐位不变；词汇表划出 已实现盈亏 ⊂ 已实现收益 ⊂ 累计收益 三层阶梯，不含浮动盈亏（价格历史只覆盖近两年）（grilling 定稿 + 原型裁决呈现形态，2026-09-19；#1533）
- 0131 — 投资页「概览」页签：全页折本位币单值口径与「投资账户现金 / 持仓市值」两腿拆分，与持仓视图「按账户币种分组、不跨币种合并」分工并存（同标签两处不同数由 scope 变体句解释）；默认落点由「盈亏」改址，可投资资产分子拆两腿、单点表达式不复制（#1536 / spec #1532，后续 #1537 补齐投资合计三项）

### 领域决策与功能边界
- 0007 — 备份/恢复采用文件级快照而非语义导出
- 0010 — 批量导入以客户端幂等键为主，内容哈希兜底；新增交易修改 API
- 0014 — 物品（Item）使用成本独立领域（#3 已被 0025 修订）
- 0015 — 「已投资 = 当前持仓」口径 + 全量修字典 / 增量刷价格切分
- 0023 — 订阅花费双口径：忠实流水 + 推算折算，不摊销
- 0024 — 定时交易 MVP 行为决策集合（部分被 0028/0042 推翻，修订注记生效）
- 0025 — 物品创建唯一入口与自包含成本（修订 0014）
- 0036 — 自建标的与手动报价：无行情来源标的的字典与价格通道
- 0037 — AI 标的搜索与幂等创建（投资迁移 HTTP 前置能力）
- 0039 — AI 基金导入：fund-lookup 只读端点与创建端点基金增强
- 0064 — 实物资产域 MVP 决策：估值档案 / 只追加估值历史 / 处置与软删 / 净资产第三腿 / 入口收纳（spec #465）
- 0119 — 信用卡账户档案：额度 / 账单日 / 还款日只作档案与展示（A 档，不做账单周期派生与提醒）、可用额度 = 额度 + 余额、派生量不落库、约束落域层不落 schema、契约体积预算提至 24KB / 50KB（spec #1327）
- 0081 — 投资标的按代码查询为主干道：股票查询端点、美股市场扩展（BREAKING）、全量同步退役、AI 导入脱钩字典
- 0095 — 标的信息同步确定进度：事件驱动、有通道分母、不做取消（在 0081 决策 3 退役旧进度事件之上重建；基金深回填页级明细已随逐只净值通道换源退役，见修订记录）
- 0103 — 行情接入接缝 QuoteAdoption：统一行情查询与建档落价（两成员、无 trait 承载），手动报价与标的信息同步留接缝外，价格写入单点改命名参数入口（spec #1010，grilling 定稿）
- 0121 — 行情日常刷新改走批量取数面：名称走全量字典、场外基金净值走排行批量面（请求量从随标的数线性增长降为常数级），逐标的通道保留为 fail-closed 降级（同步内熔断 + 跨同步记忆 + 按条缺口不熔断）、降级对用户可见，全局限速改自适应；不引入可插拔数据源抽象（grilling 定稿，2026-09-15；「不做多源」一条被 0130 取代，其余决策保留）
- 0122 — 现价刷新与价格历史解耦：历史回填改后台自动补全（无用户入口、静默标的级计数、额度让路、续做靠派生事实不落任务状态、单只回填一只一事务、持仓优先），当周采样点由现价刷新的批量取数直接落库；走势空态三态化；推翻 0019「采集单通道」口径、把页级进度明细的归属自 0095 迁至本任务（grilling 定稿，2026-09-15；部分被修订：窗口与无用户入口两条只辖标的，见修订记录）
- 0126 — 价格恒定标的：恒定是价格通道的分区成员而非通道上的正交标记（货基为首个成员，恒定单位价格落标的行、打标单向），退出采集链路（首刷队列 / 每日逐只 / 当周采样点全豁免，批量面缺口不再误计），读侧按常量取值且不落虚拟行；否决「继续采平坦序列」「正交标记」「只跳过不标记」（grilling 定稿，2026-09-17）
- 0130 — 行情与净值源策略：移除东方财富，改按链路分工选源（场内腾讯、场外基金净值新浪、判定与权威兜底证监会基金电子披露、汇率 ECB），批量面形状与降级契约沿用 0121 只换成员；保留自动识别与美股三市场闭集（不新增 us、不合并）、行情日期改取交易所当地交易日、货基判定换源且单向打标不变、fund_class 保留返回空串并弃用；零 schema 变更、零存量重写（grilling 定稿，2026-09-19）
- 0107 — 盈亏页退役逐匹配卖出明细：汇总三视图兜底、按币种分组展示，交易页新增标的下钻维度（convert 任一腿命中）与持仓下钻跳转——清仓标的卖出流水的唯一入口（grilling 定稿）
- 0090 — AI 契约双投影：标准 OpenAPI 与紧凑方言（`GET /api/v1/contract`）同一 ApiDoc 同源分流，端点级教学迁入导入知识，体积护栏 ≤22KB（spec #839）
- 0110 — 导入知识分级自足：知识索引 + 分域知识节，投资五节迁出独立端点，AI 记账升为一等场景（收窄 0090「一次拉取即自足」为契约侧价值；已发布教学正文首次做减法；#1120）
- 0042 — 定时计划自动执行（到期追补）与设备级开关
- 0051 — 保单（保险域）MVP 决策：建档 / 缴费复用订阅 / 流水直挂
- 0082 — 保司独立字典：保险公司与商户分家、保费流水不挂商户（推翻 0051 决策 7，breaking）
- 0057 — 报表页时间范围快捷选择上位：期间化口径与年份下拉退役
- 0070 — 搜索页时间范围快捷选择上位（消费形态三）：任意区间退路退役与遗留参数冻结并存
- 0091 — 多端同步：网盘哑通道 + 语义命令 OpLog 确定性重放（零丢失、单活跃设备；域引擎可编译进移动目标）
- 0097 — 投资交易删除语义修订：sell 删除回补、buy 删除级联软删在用 sell（部分修订 0013 删除条与 0033 删除入口守卫，breaking）
- 0098 — 同步触发编排：打开即同步、桌面 10 分钟低频轮询与写后 5 秒去抖即时上传（Android 只保留打开即同步）；密文库自动轮询用进程级会话口令不读钥匙串

### 机制与横切基础设施
- 0006 — tracing 生态日志基础设施（含 INFO 级隐私约定）
- 0009 — 数据库操作耗时日志：连接级 trace_v2 hook
- 0012 — 参考数据失效：事件驱动 `ledger:changed`（push-first）
- 0016 — 自动定时备份：轮询 + 写时检查（自然日语义见修订记录）
- 0017 — 后端应用配置统一存 `app_settings` KV 表（配置归口规则）
- 0018 — 数据库位置可配置：引导指针文件 + 启动期搬迁（归口规则的例外层）
- 0031 — 价格失效信号 `ledger:prices-changed`
- 0032 — 置脏触发收口连接层统一写入口（默认置脏 + 集中豁免）
- 0033 — 行为层编排协议内化：create/update/delete 三入口 + 事务所有权
- 0044 — 失效信号发射收单点：`signals_for` 映射
- 0046 — 前端包管理器切换 pnpm：严格隔离守依赖边界
- 0047 — 命令注册单一来源：注解即注册 + TS 调用面双向全等校验
- 0049 — 应用 i18n：vue-i18n Composition + 语言判定链 + 不随语言清单
- 0050 — 错误本地化契约：AppError 码化只增不改，前端按码插值
- 0054 — 写后失效信号改主线程非阻塞投递（死锁修复）
- 0056 — 后端域目录化：域/壳/基础设施三层与依赖方向守门（迁移已全部收口；分层载体已由 0112 修订为 crate 分层；守门数据分界由 #1591 定案扩充为事实投影 / 政策显式 / cargo 退役，见修订注记）
- 0059 — 模型域化：全局模型目录随域归位（修订 0056 模型定性，模型随域归属；§5 已被 0071 修订）
- 0060 — 后端易 panic 构造门禁：clippy 六件套 deny 与存量豁免安置纪律
- 0062 — 性能基准数据集：确定性生成而非入库（ledger-perf 工具，spec #458；观察期已结束，阈值门禁由 0068 另立）
- 0066 — 跨平台发布：Windows/Linux 入矩阵（x64 优先），单 Release 汇总，全平台暂不签名
- 0124 — 应用自动更新：桌面三平台 Tauri updater 自更新、GitHub Releases 静态清单、更新签名公钥烧入、全询问式不静默、无强制门槛、更新前备份接缝、壳层机制归属（收回 0066 缓行项；grilling 定稿）
- 0068 — 性能阈值与门禁：perf-bench 默认线 p95 ≤ 200ms（搜索 SQL 下推后分项例外已撤销、机制保留；兑现 0062 观察期承诺）
- 0071 — 派生缓存归域：余额与净资产口径迁出基础设施，infra→域反向依赖入守门（修订 0059 §5；认许边 crate 边界新形态见 0112 修订注记；infra→域方向的源码文本扫描退役、改按声明面判定，见 #1591 修订注记）
- 0073 — 壳层写路径仪式收敛：统一写入口 `write_entry` + 身份单点声明（声明表派生消亡，修订 0044 决策 1/3 的壳侧形态；spec #523）
- 0074 — Android 发布：GitHub Release 直装 APK、并入现有 `build.yml` 矩阵、iOS 暂缓（已落地，随移动端在 Unreleased 交付；决策 3 在 Android 侧取代 0066「全平台暂不签名」）
- 0075 — SQLite 加密模式：SQLCipher 文件级加密、主口令派生密钥、加密状态随文件走（opt-in，已落地并随 0.6.0 发布）
- 0100 — 启动期 schema 漂移守卫：init_db 尾部内存参照库方向性 diff，漂移报 `boot.schema-drift`（#971 实证，未发布迁移就地修改的防护）
- 0101 — 同步重放注册表：ReplayBinding 单点承载可重放契约、裁决域派生归域、三道门守契约（#1006；门 (b) 载体由 #1591 定案改为声明面判定，见修订注记）
- 0102 — WriteOp 闭集单一化：`write_op_set!` 声明宏同体派生 enum 本体 / `ALL` / `from_ident`，三份表示收敛为一份（#1011）
- 0108 — TransactionKind / InstrumentType 闭集单一化：共享 `closed_set!` 宏同体派生（enum / `ALL` / `as_str` / `parse` / `Display`），InstrumentType 双字符串面收口，DB CHECK 字面量测试期互核兜底（#1022）
- 0104 — 壳层读路径仪式收敛：统一读入口 `read_entry`——run_db 之上的锁仪式单点 + 源扫描守门（翻案 0073 决策 6 读侧搁置；spec #1009，grilling 定稿）
- 0105 — 交易写入协议：create/update 单正文 + Local / Replay 形态闭集（OpEmission 推广合一、delete 现状保持），守卫单点、事务自持进协议、存活校验刻意不对称（细化 0013/0033 承载；#1003/#1005 先行；spec #1004，grilling 定稿）
- 0069 — 后端 DB 调用 async 化：单连接互斥锁保留 + 统一阻塞线程池 helper（形状乙；tokio-rusqlite 与自建专用线程两否决在案，spec #498；「单连接」形态已被 0117 修订为写连接 + 固定只读连接）
- 0117 — 读路径独立只读连接（路线 B）：读走同文件只读连接 + busy_timeout，长写事务不再挡读；口令获取换连点同源成对、四条换连路径（解锁/恢复/搬迁/加密转换）成对换入换出收口既有单点、备份走 VACUUM INTO 不受影响；不含 WAL 不含连接池，WAL 升级留可检验触发条件（修订 0069 决策 1 单连接形态、推翻 #147 方案 b 的第二只读连接否决；spec #1274 决策 8 记账）
- 0120 — 多端同步轮次网络出锁：连接锁只盖数据库步骤（位点读取 / 逐条重放 / 成功时刻落库），manifest 读-改-写与同端轮次顺序性由域内轮次在途互斥承接；手动入口走分段写入口、调度侧同形分段取锁、保持逐段下载→逐段重放；失败语义按「已上传段原子 / 已重放 op 逐条原子 / manifest 回写自愈」三分重述（修订 0098 决策 2 与域间横向边的取锁口径、收窄多端同步域 SyncRound 词条持锁表述；与 0069 决策 4 收敛；issue #1281 裁决票，行为与配套文字随实施票 #1339 落地）
- 0125 — 全异步运行时形态：异步 DB 门面（专用线程 + 通道、写线程与读线程各一条；连接槽与成对构造点保持原形、门面线程独占取用，承接提交点后置 / 分段裁决 / span 归因 / 锁跨度探针；换连仍走 0117 成对原语；作业 panic 走 catch_unwind（捕获点在守卫作用域内）+ 回滚 + 通道 fail-loud 上报，回滚失败即标记连接不可信）+ 行情同步域异步化（HTTP 面换 reqwest async、编排与作用域会话接缝 async 化、后台两条车道转 async 任务、壳内阻塞包装删除）；限速器与让行异步化但保强串行（互斥体换异步形态、取消路径接受 1 个服务端重叠请求）；守门含连接槽独占禁令（豁免台账带技术原因）与生产面阻塞客户端禁令；否决「保持现状」「半程异步」「第一版连接池」「换连改作业」「第一版可撤销作业」，tokio-rusqlite 否决维持而理由改写；修订 0069 否决段「形状甲」的 ①②、承接 0117 的实现形态（spec #1406）
- 0127 — 明文库形态不变式与快照路径 ATTACH 显式 KEY：应用自有明文库保留字节 = 0（偏差是外来形态非损坏，启动引导建连前复用整库转换链归一化）、应用自写 ATTACH 显式带 KEY（`VACUUM INTO` 内部 ATTACH 唯一例外 + 快照前源库形态门禁指向重启）；含外部工具清单（Apple sqlite3 的 VACUUM 危险、`.backup` 与 Python sqlite3 模块安全）；否决 URI 参数绕过、在线备份 API、reserve_bytes pragma、备份时归一化（spec #1452，事故落档 #1455）
- 0076 — 应用更名「开源记账 / OpenLedger」：显示名与发布契约解耦，identifier 冻结（已落地并随 0.6.0 发布）
- 0080 — 依赖重启的路径改用原位重引导 + WebView 重载：进程重启退役（dev 白屏与 macOS 重启竞态，spec #644）
- 0083 — 守门脚本 TypeScript 化与 Bun 运行时：零 npm 依赖保留、类型检查入门槛（收窄范围）、门槛/前端双运行时（改判 0047「Node，无依赖」的运行时表述）
- 0085 — 前端 invoke 测试接缝唯一化：组装接线一体、全局清理与未命中报错基座、手写桩全量迁移（spec #727；同批评审姊妹 spec #728–#732）
- 0084 — 统一测试数据库工厂与共享断言库：深模块收敛、白名单守门、固定时刻显式化（spec #728，grilling 定稿；「独立测试 crate」否决前提由 0112 修订：workspace 下 dev-dependency 环可解）
- 0086 — BDD e2e 步骤共享层加深：步骤输入工厂、步骤动词走公开写入口、world 快照按域分组（spec #729，grilling 定稿）
- 0087 — 测试三层权威层：域语义归域单测、壳语义归 API 集成测试、旅程归 e2e BDD；接线证明与错误码契约代表唯一化（spec #730，grilling 定稿）
- 0111 — infra crate 内部组织与定位口径：「无域语义」细化为可守门的两条（不依赖域 crate、不定义账本数据口径），内部三区（原语 / db / boot）+ 共享接缝，crate 内分层与守门补强；壳机制暂住区已随 #1108 迁出至根包 `src/shell_support`（四区缩三区，见修订注记）（与 #1086 拆分轴分家，#1111 相邻不同轴；grilling 定稿；`INFRA_MODULES` 登记表由 #1591 定案退役、模块面改投影，见修订注记）
- 0112 — 后端 workspace crate 拆分：按域拆箱、正交判据 = 无环且方向单一、门禁保底三层（编译期依赖图 / 门禁接线负向守门 / 结构守门 crate 边界）、破环 = 协议抽离 + 一类写路径副作用接缝反转（挂载点清单；修订 0056 分层载体、0071 认许边形态、0084 测试工厂前提；否决合并大 crate 与端口/事件反转；spec #1086；`CRATES` 由 #1591 定案收窄为 crate 政策核、模块清单改投影，见修订注记）
- 0113 — 业务域 crate 内部组织：四区（共享语义 / 跨域接缝 / 写路径 / 读路径）按消费面定位、写读 → 接缝 → 共享语义单向层序与三处反边消除、模块清单双向全等与区级反向依赖守门；transaction 首个落地样板（复用 0111 的粒度规则与守门范式、把 0112 决策 1「不做域内模块重排」限缩为拆 crate 阶段纪律；grilling 定稿；zone 登记面由 #1591 定案改目录约定、模块清单 expected 改声明投影，见修订注记）
- 0118 — 前端 pnpm workspace 拆包：深模块成包裁定（成包判据 = 对壳零依赖；每深模块一包；utils 全量成包；ui-kit 成员闭集 = App* 封装族 + overlay 单例族 + 通用件四项；window-tier 构建期契约改读包路径）、模块级单例随包 ESM 持有通则、不成包深模块的守门规则⑦（深模块边界登记表，首批 TransactionFilter → views）（方法参照 0112、借方法论不涉其范围；spec #1148 / #1157，grilling 定稿）
- 0128 — 前端格式化器引入 oxfmt：独立 npm 包形态（否决 Vite+ 整体引入——内置 vitest 回退、.vue 类型检查缺口；否决 Prettier/Biome/维持无 formatter），默认 Prettier 兼容风格 printWidth 100 + sortPackageJson 关，`oxfmt --check` 入 check.sh/CI 双侧质量门 + 接线测试（删执行行即红、配置静默回退 fail-loud，oxc #25125）；pre-1.0 破例理由与重评条件在案（grilling 定稿）

### 搜索
- 0027 — 统一模糊搜索语义与实现形态（取代已删除的 0004；0043 流式化、V018 两段式与 SQL 下推均并入修订记录）
- 0008 — 交易列表服务端分页：offset 页码而非游标（缺省返回全部供 AI 读回）

### 前端深模块与交互
- 0030 — 交易列表过滤收进 TransactionFilter 深模块（URL 只读下钻）
- 0035 — 弹层抑制改响应式注册表（取代已删除的 0021，含两代嗅探事故教训）
- 0040 — 异步样板收进 Loadable 深模块（发起永不 reject）
- 0041 — 计划清单收进 ScheduledPlanList 深模块（清单/表单双接缝）
- 0045 — 交易行操作弹窗编排 TransactionModalState + 页码回退入口
- 0072 — 弹窗开启编排通用化：ModalIntent 通用工厂收编手搓弹窗三件套（0045「不做泛型」条件触发）
- 0077 — 行右键菜单编排工厂化：RowContextMenu 收编手搓定位四件套（四份同构拷贝事实与两份非行拷贝排除裁决，spec #522）
- 0058 — 字段错误态：即时红显、不拦截、提交禁用
- 0061 — 会话内保留：界面瞬态选择的会话粒度保留原则（报表页期间/下钻位置首个落地）
- 0094 — 会话内保留升为瞬态选择默认粒度：交易页筛选/分页保留与 ESC 复位通道（修订 0061 决策 2/4 逐票裁决口径、0030 事实源归属）
- 0063 — 组内收纳：每组「更多」与用户移入移出，主项 ≤3 硬上限即键位带封闭
- 0065 — 固定键位带：概览迁移到反引号键位、每组固定三键（取代 0063 决策 2 的键位推导；≤3 硬上限与组满置灰保留）
- 0078 — 危险操作确认应用内分级：原生 confirm 清零与 error/warning 两级形态（恢复入口按钮降权同批裁决，grilling 定稿）
- 0079 — 对话框排版规范：卡片外观统一——cardSize 分档宽度、默认无边框、表单/按钮行/辅助文案收口进 AppModal（spec #630；issue #641 原写作 ADR-0078，编号顺延）
- 0088 — 移动端 UI 适配：两轴模型（宽度定布局、输入定交互）、840dp 单断点、导航抽屉、系统返回第四关闭通道与 10 票分期（已落地，随移动端在 Unreleased 交付；grilling 定稿）
- 0093 — 前端自定义样式引入 vanilla-extract 原子样式方案
- 0107 — 盈亏页退役逐匹配卖出明细：汇总收敛为按年/按账户/按标的三视图并按币种分组（废止跨币种混算），交易页 TransactionFilter 增标的维度 URL 下钻（持仓下钻与清仓标的入口；关联 0030/0094）
- 0123 — push-first 清单生命周期四店塌缩为 createPushFirstList 工厂：load 快照 + apply 落位、ledger:changed 订阅内化、写后重拉失败统一不反转写动作成败（items/policies 窄窗口误报「保存失败」随之消失）、physicalAssets 筛选切换在途竞态保真迁移另立票、机制测试钉工厂单点店测试退化为领域动作；不成包（消费面全在壳，viewResetRegistry 先例）；机制术语不进词汇表（grilling 定稿）

### 界面组织
- 0022 — 设置页 Tab 按领域分域（部分被修订，「分类与币种」已更名「分类」）
- 0116 — 功能开关：关闭只隐藏入口（不删数据、不停能力），与组内收纳两层正交；可选清单为开发者策展闭集九项、设备级持久化、入口消失而引用放行；设置页新增「功能」Tab（修订 0022；grilling 定稿，2026-09-13；spec #1237）

## 已删除（tombstone）

编号保留检索用；删除依据：被后续 ADR 完整转述、决策从未落地、或无真实取舍。

- **0002** 独立模型每类型 — 同日被 0003 取代，0003 背景已完整转述翻案过程
- **0004** FTS5 交易搜索 — 被 0027 取代，FTS5 机制已整体移除，现行语义以 0027 为准
- **0005** 两级分类 NDataTable 方案 — 决策从未落地（实际用 NTree）；「两级封顶」约束由领域模型承载（CONTEXT-core Category）
- **0021** 弹层遮罩嗅探 — 机制被 0035 取代，事故教训已在 0035 背景完整重述
- **0034** 移除币种只读展示 / Tab 更名 — 纯 UI 清理，结论见 0022 修订注记与根 README
- **0043** 搜索候选流式化 — 要点（流式决策、实测数据、不预留索引逃生舱）已并入 0027 修订记录
- **0055** 低频视图收纳策略（全局单一「更多」） — 被 0063 组内收纳取代：决策 1/2 后半/5 由 0063 取代，决策 2 前半（开发者清单）与决策 4（存量路由重定向先例）由 0063 决策 3/5 继承，页签容器形态由 0063 决策 1 继承；键位推导余绪再由 0065 取代
